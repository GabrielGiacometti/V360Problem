

<div class = 'sub-container'>
    <%= check_box_tag "check_#{sub.id}", "true", sub.done, class: 'check' %>
    
    <h2 class = "sub-title"><%= sub.title %></h2>
    <%= button_to "x", sub, class: 'sub-destroy', method: :delete, remote: true, data: { type: :json, turbo_method: 'delete', turbo_confirm: "Do you really want to delete all the tasks?" } %>
    <%= button_to "Delete Task", task_path(sub), method: :delete, remote: true, data: { type: :json} %>

    <%= button_to task_path(sub), method: :get, remote: true,class: "sub-show",id: "botao_#{sub.id}" do %>🕮<% end %>
</div>

