
<div id="<%= dom_id task %>" class ="show_task ">
      <div class ='card'>
        <%= check_box_tag "check_#{task.id}", "true", task.done,class: 'check' %>
        <label for= "check_<%= task.id %>" class= "title"><%= task.title %></label>
        <div class='number-sub'><label for="botao_<%= task.id%>">sub tasks <%=task.sub_task.size%></label></div>
        <!-- <%= button_to new_sub_task_path(id: task.id), method: :get, class: "button" do %>&#x1F7C4;<% end %>-->
        <%= button_to task_path(task), method: :get, class: "button-sub",id: "botao_#{task.id}",data:{turbo_frame:"_top"} do %>🕮<% end %>

    </div>
    
    <button class="button-description" >&#128065</button>
    <p class= 'description'><%= task.description %></p>
    
</div>
